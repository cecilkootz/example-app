[web]
webserver1.codomain.com ansible_user=deploy

[all:vars]
ansible_python_interpreter=/usr/bin/python3

; Run with: ansible-playbook -i inventory.ini site.yml --ask-pass --ask-become-pass
; (passwords are prompted; nothing is stored in the repo)